#[cfg(test)]
mod matrix_tests {


    //use cranium_rs::matrix::*;

    #[test]
    fn test_create_matrix() {
        //let x = create_matrix(1, 1, Rc::new(RefCell::new(Vec::new())));
        //println!("{:?}", x)
    }
}